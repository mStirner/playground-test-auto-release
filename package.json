{
  "name": "playground-test-auto-release",
  "version": "1.3.0",
  "description": "Playground repo for testing automatic github releases",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "./node_modules/.bin/eslint .",
    "build": "grunt build",
    "compress": "grunt compress",
    "checksum": "grunt checksum",
    "release": "grunt release",
    "publish": "grunt publish",
    "dev": "./node_modules/.bin/nodemon -w index.js",
    "clean": "rm -rf ./build && rm -rf ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mStirner/playground-test-auto-release.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mStirner/playground-test-auto-release/issues"
  },
  "homepage": "https://github.com/mStirner/playground-test-auto-release#readme",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "grunt": "^1.6.1",
    "grunt-contrib-uglify": "^5.2.2"
  }
}